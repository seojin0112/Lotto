[범례] - 문제점 / + 해야 할 것 / = 해결 / ? 미정

26.08.24
= 세금 계산기 제거, 기대값 표에 "세금 포함" 토글로 통합(등수별 기대 당첨금에 원천징수 적용) — 탭명도 [기대값]으로 변경, 구 #tax 링크는 #ev로 호환
= 인기도 순 목록에 "N개 조합 보기" 추가 — 해당 점수 구간을 전 조합에서 실시간 재열거해 60개씩 이어서 표시(작은 티어는 전체 표시)
= 조합 호버 시 인기도 지도에 해당 번호 강조 + 본문 지도가 화면 밖이면 우하단 플로팅 미니 지도 표시
= 전 조합 스캔·열거의 청크 예약을 setTimeout→MessageChannel로 교체 — 백그라운드 탭 타이머 스로틀(1Hz)로 스캔이 수 분씩 걸리던 문제 해결
= 페이지를 3개 탭으로 개편 — [번호 변환] [조합 인기도] [기대값·세금], URL 해시(#pop/#tax)로 탭 유지
= 조합 인기도 탭 확장 — 숫자별 인기 지도(1~45 실측 통계, 붉/푸른 색상), 조합 인기도 검색(배수·백분위·특징 분해·예상 기대값), 전 조합 스캔을 인기도 순 순위 목록(정렬 전환·더 보기)으로 개편
= 기대값·세금 탭 추가 — 인기도별 1,000원당 예상 기대값 표(최근 52회 평균 파라미터, 동시 당첨 분할 반영), 당첨금 세금 규칙 설명·계산기(200만 이하 비과세, 3억 이하 22%/초과 33%), 당첨 이력 금액에 세후 실수령 툴팁
= 캐시된 구버전 draws.js와 새 코드가 섞이면 NaN·이력 크래시가 나던 문제 수정 — 스키마(열 수) 검증 후 불일치 시 강력 새로고침 안내 표시
= 조합 인기도 추정 기능 추가 — 1등 수동·반자동 당첨자 수(262회~)와 판매량의 푸아송 회귀(popularity.js)로 조합별 상대 인기도를 추정. 변환 결과 게임마다 인기도 뱃지(배수·백분위), 당첨 이력에 등수별 당첨자 수·1등 자동/수동 구분·해당 회차 조합 인기도 표시, 전 조합(814만) 스캔으로 인기/비인기 유형 둘러보기 섹션 추가
= draws.js 스키마 확장 — 등수별 당첨자 수, 총판매액, 1등 자동/수동/반자동 수집(update-draws.mjs), 전 회차 재수집

26.08.10
= 당첨 이력 조회 기능 추가 — 변환된 번호를 역대 전 회차(1~1236회)와 대조해 등수·당첨금·해당 회차 당첨번호(일치 번호 강조)를 표시, 몇 등까지 조회할지 필터 지원
= 당첨번호 데이터 파이프라인 구축 — scripts/update-draws.mjs가 동행복권 API에서 등수별 실제 당첨금 포함 전 회차를 수집해 draws.js 생성, GitHub Actions(update-draws.yml)로 매주 일요일 00:30 KST 자동 갱신

26.08.08
= 로또 번호 변환기 초기 구현(index.html) — 입력 문자열을 SHA-256 해시로 1~45 중 6개 번호에 결정론적으로 치환(같은 입력이면 항상 같은 번호), 게임 수 1~5 선택 가능, 결과 복사·다크모드 지원
? GitHub Pages 배포 여부
